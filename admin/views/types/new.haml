%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:types, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:types, :new)

.tabs-content
  = form_for :type, url(:types, :create), :class => 'form-horizontal' do |f|
    =partial 'types/form', :locals => { :f => f }
