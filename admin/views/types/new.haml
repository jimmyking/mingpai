%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, "列表"), url(:types, :index)
  %li.active=link_to tag_icon(:plus, "新增"), url(:types, :new)

.tabs-content
  = form_for :type, url(:types, :create), :class => 'form-horizontal' do |f|
    =partial 'types/form', :locals => { :f => f }
