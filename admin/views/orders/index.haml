%ul.nav.nav-tabs
  %li.active=link_to tag_icon(:list, pat(:list)), url(:orders, :index)
  %li=link_to tag_icon(:plus, pat(:new)), url(:orders, :new)

.tabs-content
  %table#list.table.table-striped.table-hover.table-condensed
    %thead
      %tr
        %th.header.list-menu
          .dropdown
            %a.list-menu-toggle.dropdown-toggle{:href => '#', :'data-toggle' => :dropdown}
              =tag_icon :magic
              %span.caret
            %ul.list-menu.dropdown-menu{:role => :menu}
              %li.list-menu-wrapper=link_to tag_icon(:check, pat(:select_all)), '#', :id => 'select-all', :class => 'list-menu-link'
              %li.list-menu-wrapper.list-menu-wrapper-disabled
                =link_to tag_icon(:'check-empty', pat(:deselect_all)), '#', :id => 'deselect-all', :class => 'list-menu-link list-menu-link-disabled'
              %li.list-menu-divider.divider
              %li.list-menu-wrapper.list-menu-wrapper-disabled
                =link_to tag_icon(:trash, pat(:delete_selected)), '#', :id => 'delete-selected', :class => 'list-menu-link list-menu-link-disabled'
                .list-menu-popover-delete-selected.popover.right
                  .arrow
                  %h3.popover-title=pat(:delete_selected_title)
                  .popover-content
                    = form_tag url(:orders, :destroy_many), :method => :delete do
                      =hidden_field_tag :order_ids, :'data-delete-many-ids' => true
                      =submit_tag pat(:delete), :class =>'list-menu-popover-delete-selected-btn btn btn-danger btn-small'
                      .btn.btn-default.btn-small.cancel=pat(:cancel)
        %th.header= mat(:order, :id)
        %th.header= mat(:order, :order_no)
        %th.header= mat(:order, :no)
        %th.header= mat(:order, :type_id)
        %th.header= mat(:order, :order_status_id)
        %th.header= mat(:order, :order_process_id)
        %th.header= mat(:order, :department_id)
        %th.header= mat(:order, :acter)
        %th.header= mat(:order, :level)
        %th.header= mat(:order, :demand)
        %th.header= mat(:order, :acter_account)
        %th.header= mat(:order, :acter_pw)
        %th.header= mat(:order, :wangwang)
        %th.header= mat(:order, :mobile)
        %th.header= mat(:order, :qq)
        %th.header= mat(:order, :amount)
        %th.header= mat(:order, :paytype)
        %th.header= mat(:order, :memo)
        %th.header= mat(:order, :created_at)
        %th.header= mat(:order, :updated_at)
        %th.header.list-row-action-header

    %tbody
      -@orders.each do |order|
        %tr.list-row
          %td.list-column.list-selectable
            =check_box_tag 'order_ids[]', :value => order.id, :class => 'list-selectable-checkbox'
          %td.list-column=order.id
          %td.list-column=order.order_no
          %td.list-column=order.no
          %td.list-column=order.type_id
          %td.list-column=order.order_status_id
          %td.list-column=order.order_process_id
          %td.list-column=order.department_id
          %td.list-column=order.acter
          %td.list-column=order.level
          %td.list-column=order.demand
          %td.list-column=order.acter_account
          %td.list-column=order.acter_pw
          %td.list-column=order.wangwang
          %td.list-column=order.mobile
          %td.list-column=order.qq
          %td.list-column=order.amount
          %td.list-column=order.paytype
          %td.list-column=order.memo
          %td.list-column=time_ago_in_words order.created_at
          %td.list-column=time_ago_in_words order.updated_at
          %td.list-column.list-row-action
            .list-row-action-wrapper
              =link_to tag_icon(:edit), :href => url(:orders, :edit, :id => order.id), :rel => :tooltip, :title => "#{pat(:edit)} order", :class => 'list-row-action-wrapper-link'
              =link_to tag_icon(:trash), :rel => :tooltip, :title => "#{pat(:delete)} order", :class => 'list-row-action-delete-one list-row-action-wrapper-link'
              .popover.list-row-action-popover-delete-one.left
                .arrow
                %h3.popover-title=pat(:delete, :model => "order")
                .popover-content
                  = form_tag url(:orders, :destroy, :id => order.id), :method => :delete do
                    =submit_tag pat(:delete), :class =>'list-row-action-popover-delete-one-btn btn btn-danger btn-small'
                    .btn.btn-default.btn-small.cancel=pat(:cancel)
