%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:roles, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:roles, :new)

.tabs-content
  = form_for :role, url(:roles, :create), :class => 'form-horizontal' do |f|
    =partial 'roles/form', :locals => { :f => f }
