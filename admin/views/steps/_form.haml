- error = @step.errors.include?(:name)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :name, :class => 'control-label'
  .controls
    =f.text_field :name, :class => 'form-control input-large input-with-feedback', :autofocus => true
    %span.help-inline=error ? f.error_message_on(:name, :class => 'text-error') : pat(:example)
- error = @step.errors.include?(:sort)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :sort, :class => 'control-label'
  .controls
    =f.text_field :sort, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:sort, :class => 'text-error') : pat(:example)
- error = @step.errors.include?(:value)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :value, :class => 'control-label'
  .controls
    =f.text_field :value, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:value, :class => 'text-error') : pat(:example)
- error = @step.errors.include?(:type_id)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :type_id, :class => 'control-label'
  .controls
    =f.text_field :type_id, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:type_id, :class => 'text-error') : pat(:example)

.form-actions
  =f.submit pat(:save), :class => 'btn btn-primary'
  &nbsp;
  =f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue'
  &nbsp;
  =link_to pat(:cancel), url(:steps, :index, :type_id => params[:type_id]), :class => 'btn btn-default'